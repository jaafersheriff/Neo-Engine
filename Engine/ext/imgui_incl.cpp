#include "ext/imgui_incl.hpp"

// Nothing yet - this exists so imgui_config.hpp's ImTextureID type has a translation unit of its own
// in the imgui target, and so the header above has somewhere to grow an out-of-line helper.
